# 자동결제 운영·사용성 로드맵

2026-09-14 · 우선순위 기준: 금액 사고 가능성 → 고객 이탈 가능성

이 문서는 자동결제 MVP 코드 리뷰에서 확인한 고객 여정과 운영 과제를 실행 순서로 관리합니다. 완료 표시는 Git에 구현과 검증이 함께 반영된 경우에만 변경합니다.

## 카드 운영 원칙

- Workspace마다 활성 Toss 카드는 **주 카드 1개 + 백업 카드 1개**, 최대 2개입니다.
- 첫 등록 카드는 주 카드, 두 번째 등록 카드는 백업 카드가 됩니다.
- owner/admin이 백업 카드를 주 결제수단으로 바꿀 수 있으며, 전환은 한 DB 트랜잭션과 감사 로그로 남깁니다.
- 기존 주 카드에 결과가 확정되지 않은 승인이 있으면 결과 반영 전까지 전환을 차단합니다.
- Worker는 주 카드만 사용합니다. 백업 카드는 별도 고객 동의 없이 자동 대체 승인하지 않습니다.
- 카드 등록 실패 시 기존 주 카드와 백업 카드는 그대로 유지합니다.
- 카드 삭제·교체와 자동 대체 승인 정책은 별도 확정 후 구현합니다.

## 요금제 변경 원칙

- [x] 라이트·베이직·프로를 마이페이지에서 선택하고 변경 조건에 동의
- [x] 기본 이용료와 VAT 10%를 분리 표시하고 월 총 청구액까지 동의에 저장
- [x] KST 기준 다음 달 1일 예약 적용, 기존 Invoice 금액과 정책 snapshot 보존
- [x] 예약 교체 시 이전 예약 취소, 최신 예약 한 건만 유지
- [x] 엔터프라이즈는 자동 변경하지 않고 별도 견적 문의로 연결
- [x] 요금제 변경 동의와 카드 등록 동의를 별도 기록으로 관리
- [x] 카드 등록·기존 고객 자동결제 전환 여부와 무관하게 요금제 변경 동의와 예약 허용

확정 카탈로그는 라이트 590,000원/200건, 베이직 990,000원/500건, 프로 1,790,000원/1,000건, 엔터프라이즈 2,000건 이상/별도 견적이며 VAT 별도입니다. 변경일에 새 구독 조건을 적용한 뒤 생성되는 Invoice부터 새 금액을 사용하며 일할 계산은 하지 않습니다.

## Phase 0 — 자동 승인 활성화 전

- [ ] `billing_events` 기반 결제 실패·최종 실패·카드 등록 완료 이메일
- [ ] `billing.charge_requires_review`, 장기 reconciliation, 고아 빌링키 정리 실패의 Sentry 또는 Slack 알림
- [ ] 기술 오류 10회 차단·미납 Invoice 조회와 `billing_approve_arrears_retry`를 제공하는 최소 운영자 화면

고객 알림 수신처는 `customers.billing_email`을 사용하되, 발송 전 주소 유효성과 수신 정책을 확인합니다. 알림 이벤트에는 `notified_at`, 발송 결과, 재시도 횟수를 남겨 중복 발송을 막습니다.

## Phase 1 — 첫 고객 등록 전

- [x] callback 독립 화면 스타일, 서버 오류 표시, 성공 후 이용 플랜 자동 복귀
- [x] `/mypage?section=plan` 진입 지원
- [x] 동의 화면 닫기와 Toss SDK 로딩 상태 표시
- [x] `contract_date` 정책의 최초 결제일 미확정 시 등록 차단
- [x] 카드 등록 실패 시 기존 결제수단 유지 안내
- [x] 주 카드·백업 카드 표시, 최대 2장 제한, 주 카드 전환
- [x] 플랜 전환 미확정 상태를 등록 버튼 전에 안내

## Phase 2 — 첫 결제 실패 전

- [ ] 실패 행에 `카드 변경`과 `담당자에게 문의` 조치 버튼
- [ ] viewer/editor에게 owner/admin 확인 경로와 담당자 정보 제공
- [ ] 모바일 결제내역 카드형 보기
- [ ] 결제정보 스켈레톤과 조회 실패 재시도 버튼
- [ ] 날짜·시간 표기 기준 통일

## Phase 3 — 거래량 증가 전

- [ ] Toss webhook을 결제·취소 상태 반영 가속기로 연결하고 현재 복구 Worker는 유실 안전망으로 유지
- [ ] 카드 만료 정보를 제공하는 Toss 응답 확인 후 저장·만료 30일 전 안내
- [ ] 실제 Supabase migration 전체 체인을 순서대로 적용하는 PGlite 테스트
- [ ] 고객/운영자 환불 실행 경로와 승인·감사 정책
- [ ] 카드 삭제·교체 및 백업 카드 자동 대체 승인 여부의 사업 정책 확정

## 외부 키 수령 후 확인

- Toss Test 환경에서 첫 카드가 주 카드, 두 번째 카드가 백업 카드로 남는지 확인
- 주 카드 전환 직후 새 Invoice Attempt가 선택된 카드 credential을 사용하는지 확인
- 세 번째 카드 등록이 Toss 인증 전에 차단되는지 확인
- 실제 Supabase/PostgREST에서 카드 목록 정렬과 주 카드 전환 RPC 권한을 owner/admin/editor/viewer별로 확인
- 카드 등록 callback 성공·실패·취소와 자동 복귀를 모바일/데스크톱에서 확인
- 라이트·베이직·프로 변경 동의와 다음 달 1일 적용을 실제 Supabase에서 확인
- 적용 직전·직후 Cron 중복 호출에도 한 번만 반영되고 기존 Invoice가 유지되는지 확인
